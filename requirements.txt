# Install with: pip install -r requirements.txt
# tshark / mergecap are NOT pip packages - install Wireshark separately.
# Pinned versions verified working together on Python 3.11 with tshark 4.2.2.
# Note: manuf 1.1.5 has an old build backend; on some systems you may need
#   pip install --no-build-isolation manuf
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.9.0
scipy==1.17.1
torch==2.12.1
scapy==2.7.0
plotly==6.8.0
dash==4.3.0
dash-bootstrap-components==2.0.4
manuf==1.1.5
